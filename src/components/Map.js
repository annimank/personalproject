import React, {useState} from 'react';
import { Box, Tabs, Tab, Typography } from '@mui/material';

function Map() {

    return (
        <Box paddingTop='100px'>
            <Typography variant='h1' textAlign='center' >FISHING MAP</Typography>
        </Box>
    )
}

export default Map;